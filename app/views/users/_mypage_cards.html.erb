<ul class="list-group list-group-flush">
  <div class="container">
    <div class="row justify-content-md-center">
      <% @favorites.each do |favorite| %>
        <% court = Court.find_by(id: favorite.court_id)%>
        <div class="col-md-10 court-card">
          <div class="card mb-10 shadow-sm">
            <div class="card-body">
              <span class="card-left">
                <p class="card-header">
                  <span class="card-title"><%= court.name %></span>
                  <% if @current_user %>
                    <span class="card-favorite-id-<%= court.id %>">
                      <% if Favorite.find_by(user_id: @current_user.id, court_id: court.id) %>
                        <%= render partial: "home/destroy_favorite", locals: {court: court} %>
                      <% else %>
                        <%= render partial: "home/create_favorite", locals: {court: court} %>
                      <% end %>
                    </span>
                  <% end %>
                </p>
                <p class="card-description">
                  <%= "最寄り駅: #{court.station}" %>
                </p>
                <span class="card-address">
                  <%= "#{court.address}" %>
                </span>
                <%= render partial: "home/table", locals: {court: court} %>
              </span>
              <div class="card-counter">
                <p class="card-counter000">
                  <%= "最寄り駅: #{court.station}" %>
                </p>
                <span class="card-counter111">
                  <%= "最寄り駅: #{court.station}" %>
                </span>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</ul>