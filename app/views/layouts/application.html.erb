<!DOCTYPE html>
<html>
  <head>
    <title>バスケットマニア</title>
    <%= include_gon %>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <!-- Sprocketsでjavascriptを読み込ませるタグ -->
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %> 
    <!-- Webpackでjavascriptを読み込ませるタグ -->
    <%= javascript_pack_tag 'index' %> 
    
  </head>
  <body>
    <header>
      <div class="navbar navbar-dark bg-dark shadow-sm">
        <div class="container d-flex justify-content-between">
          <a href="/" class="navbar-brand d-flex align-items-center">
            <i class="fas fa-basketball-ball"></i>
            <strong>バスケットマニア</strong>
          </a>
          <div class="btn-toolbar" role="toolbar" aria-label="Toolbar with button groups">
            <% if @current_user %>
              <div class="btn-group mr-2" role="group" aria-label="First group">
                <%= link_to 'マイページ', "/users/#{@current_user.id}", class: "btn btn-outline-primary" %>
              </div>
              <div class="btn-group mr-2" role="group" aria-label="Third group">
              <%= link_to 'Log Out', "/logout", class: "btn btn-outline-danger", method: :get %>
            </div>
            <% else %>
            <div class="btn-group mr-2" role="group" aria-label="First group">
              <a class="btn btn-outline-primary" href="/login">Log In</a>
            </div>
            <div class="btn-group mr-2" role="group" aria-label="Second group">
              <a class="btn btn-outline-primary" href="/users/new">Sign Up</a>
            </div>
            <% end %>
          </div>
        </div>
      </div>
    </header>
    <% if flash[:notice] %>
      <div class="alert alert-success" role="alert">
        <%= flash[:notice] %>
      </div>
    <% end %>
    <%= yield %>
    
  </body>
  <footer class="footer">
    <p>Blog template built for <a href="https://getbootstrap.com/">Bootstrap</a> by <a href="https://twitter.com/mdo">@mdo</a>.</p>
  </footer>
</html>